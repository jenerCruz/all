/* gist-helper.js
 * Pequeño wrapper reutilizable para subir/descargar JSON a GitHub Gist con backoff
 * Requiere: idb y config guardada en store 'config' key='gist' con {id, token}
 */
(async function(){
  // Este fichero es opcional. El evidence-module contiene ya la función.
})();